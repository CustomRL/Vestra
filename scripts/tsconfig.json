{
  "//": "Tooling scripts are never published, so top-level await is fine here — the no-top-level-await rule applies to packages/*/src only.",
  "extends": "../tsconfig.base.json",
  "compilerOptions": {
    "noEmit": true,
    "rootDir": ".",
    "module": "nodenext",
    "types": ["node"]
  },
  "include": ["**/*.ts"],
  "//references": "A script that imports a workspace package needs the reference, or its types resolve to `error` until something has built `dist`. CI lints before it builds, so without these the benchmark passed locally and failed there.",
  "references": [
    { "path": "../packages/types" },
    { "path": "../packages/rest" },
    { "path": "../packages/gateway" },
    { "path": "../packages/core" }
  ]
}
