/**
 * Vestra — a lightweight, fully-typed Discord library for Node.js.
 *
 * This package is a re-export of `@vestra/core` and contains no logic of its own,
 * so that `npm i vestra` is the one-line install while the underlying packages stay
 * independently consumable.
 *
 * @packageDocumentation
 */

export * from '@vestra/core'
