{
  "name": "@vestra/gateway",
  "version": "0.0.0",
  "description": "Discord gateway client: sharding, resuming, transport compression",
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=22.15.0"
  },
  "sideEffects": false,
  "files": [
    "dist"
  ],
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc --build",
    "clean": "rm -rf dist *.tsbuildinfo",
    "check:packaging": "publint --strict && attw --pack . --profile esm-only"
  },
  "keywords": [
    "discord",
    "discord-api",
    "bot",
    "typescript"
  ],
  "dependencies": {
    "@vestra/types": "workspace:^"
  },
  "devDependencies": {
    "@types/node": "26.2.0"
  }
}
